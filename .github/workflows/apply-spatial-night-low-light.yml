name: apply spatial night low light

on:
  push:
    branches:
      - feat/spatial-night-low-light
    paths:
      - .github/workflows/apply-spatial-night-low-light.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: feat/spatial-night-low-light

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Apply Night Low Light patch
        run: python .github/apply-night-low-light.py

      - name: Verify build
        run: |
          npm ci
          npm run build

      - name: Commit implementation and remove temporary patch files
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add AGENTS.md docs src .github/spatial-browser-check.mjs
          git rm .github/apply-night-low-light.py .github/workflows/apply-spatial-night-low-light.yml
          git commit -m 'Add Night Low Light spatial candidate'
          git push origin HEAD:feat/spatial-night-low-light
