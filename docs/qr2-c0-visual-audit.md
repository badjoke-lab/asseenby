# QR2 C0 rendered visual audit

Status: **IN PROGRESS / VISUAL FAIL / DO NOT MERGE AS QR2-COMPLETE**

## PR #53 generated candidate

PR #53 was merged to `main` as `9df6ef6e4014368cc787e532d0da9bd594890a9d` and the canonical Blender workflow then regenerated the authored C0 source/runtime asset as commit `235dad97fcb700a2913092f74ee3a9594c4b8d44`.

The generated runtime GLB now has SHA-256 `8d6f567be0de2873073ffa6671ec8d2d4da503a1e440bf6317fabee5fb943976` and includes the new reference-perimeter facade source changes from PR #53. Because the generated-asset commit was made by the workflow token, this documentation commit intentionally follows it so the normal main build/browser/production verification workflows run against the actual generated GLB rather than only the source-script merge.

The PR's pre-merge spatial browser run was **not green**: it timed out while selecting `#spatial-scene-select` during the Night Intersection -> Photo Reference lifecycle step. Therefore the PR description's statement that rendered browser checks succeeded is not treated as evidence. Fresh browser and production verification on the generated GLB is required.

## September 2026 reference mismatch review

The six fixed 82° Hansaplatz plates were re-reviewed against the checked-in
browser artifacts before the next authored rebuild. The highest-impact mismatches
are, in priority order:

1. the photographic skyline is dominated by occupied five-to-six-storey perimeter
   facades, while the authored reconstruction is dominated by low pavilion boxes;
2. photographic windows have deep masonry heads, sills, frames and varied lit-room
   states, while the current elevations read as flat repeated panels;
3. the reference has a continuous, irregular ground-floor shopfront rhythm rather
   than a few uniform emissive bays;
4. facade cornices, floor bands, balconies and entrance projections create strong
   grazing-light depth that is absent from the current silhouette;
5. mature tree canopies overlap several facade storeys, while the authored planting
   scale and distribution remain sparse;
6. plaza furniture, bicycles, utility cabinets, tree works and parked vehicles make
   the reference lived-in; the current open areas remain visually under-populated;
7. the reference pavement changes module and tone between carriageway, plaza and
   storefront apron, while the authored paving remains too uniform;
8. storefront and street-lamp practicals create pools of light and facade falloff;
   the current lighting remains comparatively flat;
9. distant rooflines and street exits close the reference composition, while the
   authored chunk exposes abrupt, empty edges.

The current source increment addresses items 1–4 first in Blender by adding two
reference-perimeter elevations behind the low shopping-centre layer. Each elevation
uses recessed room/window assemblies, real lintel/sill/mullion geometry, projecting
floor bands and cornices, varied occupied-room emission and a continuous authored
shopfront. It deliberately does not add primary-visible geometry to Three.js.

This source increment has now been regenerated into the canonical `.blend` and GLB.
It is still **not a quality pass** until fresh desktop/mobile and reference-paired
screenshots from the generated candidate are inspected. QR2 remains open regardless
of object count or successful export.

Latest previously inspected browser run: `34247303806` on the authored-prop candidate. The run was technically green and its forward, turned, translated, opposite and mobile screenshots were manually inspected.

The previous Blender-generated refinement candidate was `848a9c4f8ba6b736f7c9714f33fc40d9849952b7`, generated by `Build Blender C0 visible core` run `34250084381`. It is superseded for current visual review by generated commit `235dad97fcb700a2913092f74ee3a9594c4b8d44`.

## What the latest inspected authored-prop run proved

- The authored C0 GLB loaded through the real chunk runtime.
- Human look controls remained active.
- Bounded ground movement remained active; the browser proof translated the Human observer after a real W input.
- The CC0 authored street-prop replacement pass rendered without browser failures.
- Desktop and mobile browser regression remained usable.

That technical success is **not QR2 acceptance**.

## Rendered failure observed in run `34247303806`

The authored-prop candidate still failed the product-quality gate in Normal mode:

- primary buildings still read as large rectangular brick masses with insufficient macro-articulation;
- repeated window rows still read as flat/repetitive blue panels rather than varied occupied architecture;
- storefront fronts remained shallow and visually weak despite the first interior-depth pass;
- the turned/moved viewpoints exposed very large flat side facades;
- a jacaranda placement visibly intersected/pressed into a primary facade because its prior hard-coded position sat inside the building footprint;
- seating was also too close to the building footprint rather than clearly grounded in the sidewalk strip;
- street and curb surfaces remained too clean/sparse, with too few drains, covers, patches and close-range scale cues;
- lighting still lacked enough contact/occlusion depth for a convincing night street;
- overall composition still read as a staged low-detail intersection rather than a believable lived-in authored environment.

## Current reference-perimeter candidate

The active Blender reconstruction now combines the low Hansaplatz shopping-centre/pavilion layer with two taller occupied perimeter elevations derived from the photographic skyline mismatch review.

It now includes:

- the existing CC0-authored street props and corrected tree/seating placement;
- scanned Hansaplatz-matched PBR material inputs;
- low pavilion, canopy, plaza and transit-entry reconstruction;
- two taller perimeter elevations behind that layer;
- recessed varied room/window assemblies with dark, warm and cool occupied/unoccupied states;
- real lintel, sill, mullion, belt-course, cornice and parapet geometry;
- continuous authored ground-floor shopfront strips;
- all primary-visible additions authored in Blender/GLB rather than Three.js procedural geometry.

## Remaining blockers to judge in the next render

The next screenshot audit must decide, from actual forward/turned/translated/opposite/mobile views, whether:

- the new tall perimeter layer actually matches the reference composition rather than reading as another generic facade;
- facade articulation is visibly strong enough at walking distance;
- tree/seating grounding remains correct after the macro reconstruction;
- window variation stops the facade from reading as a repeated grid;
- storefronts have enough construction and depth to survive close approach;
- street microdetail remains visible at the user camera scale;
- side-facade detail fixes the most obvious flat-wall failure;
- the night-light hierarchy still needs a separate shadow/occlusion/runtime-light pass;
- chunk edges and distant exits still expose empty-world boundaries.

If those screenshots still read as cheap low-poly or placeholder work, QR2 remains failed and the next Blender pass will continue architecture/storefront/material/lighting work. Do not advance to QR3 or E5 based on CI alone.

QR2 passes only when representative walking-distance screenshots no longer read as debug, cheap low-poly, or placeholder work. A green build/browser test is necessary but not sufficient.
